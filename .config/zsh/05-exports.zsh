export ESCDELAY=0

export PAGER=less
export LESS='-X -F -i -R -s -S'
export LESSHISTFILE=-

export EDITOR=nvim
export MANPAGER=$EDITOR\ +Man!

export RIPGREP_CONFIG_PATH=$HOME/.config/ripgrep/ripgreprc

export FZF_DEFAULT_OPTS='--tiebreak=end --layout=reverse --no-mouse --no-multi --hscroll-off=13' # --bind alt+space:jump-accept

export GPG_TTY=$TTY

export MAIL=~/mail
