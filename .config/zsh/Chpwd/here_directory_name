#autoload
emulate -L zsh

case $1 in
n)
	local dir
	case $2 in
	here)
		read -r dir <$_ZSH_HERE_FILE ;;
	*)
		return 1 ;;
	esac

	typeset -ga reply
	reply=( $dir )
	return 0 ;;

c)
	_wanted dynamic-dirs expl 'here directory' compadd -S\] -- here
	return ;;
esac

return 1
